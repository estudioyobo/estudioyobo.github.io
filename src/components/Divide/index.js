import styled from "styled-components";

const Divide = styled.div`
  display: flex;
  align-items: center;
  margin-top: -4px;
  p {
    padding-left: 20px;
  }
  img {
    width: 50%;
  }
`;

export default Divide;
